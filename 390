word = input()

# Знаходимо найменшу кількість літер, які треба дописати
for i in range(len(word)):
    # Перевіряємо, чи частина слова від i до кінця утворює паліндром
    if word[i:] == word[i:][::-1]:
        # Кількість літер, які треба додати
        print(i)
        break
