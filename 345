# 1. Введення рядка
input_string = input("Введіть рядок тексту: ")

# 2. Введення кількості символів для перетворення (n)
# Додамо перевірку, щоб забезпечити введення цілого числа
try:
    n = int(input("Введіть кількість символів (n) для верхнього регістру: "))
except ValueError:
    print("Помилка: n має бути цілим числом.")
    # Припиняємо виконання, якщо введені некоректні дані
    exit()

# 3. Обробка рядка
# Переконуємося, що n не перевищує довжину рядка
if n > len(input_string):
    n = len(input_string) # Обмежуємо n довжиною рядка

# Ділимо рядок на дві частини за допомогою зрізів (slicing)
# Частина 1: Перші n символів, перетворені у верхній регістр
first_part = input_string[:n].upper()

# Частина 2: Решта рядка (починаючи з індексу n)
second_part = input_string[n:]

# 4. Виведення результату
result_string = first_part + second_part
print("\nВихідні дані:")
print(result_string)
